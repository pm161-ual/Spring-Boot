name: Deploy
on:
  push:
    branches: [ main ] # Se ejecuta cuando subes algo a main
  workflow_dispatch:   # Esto permite ejecutarlo a mano con un botón

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Build with Maven
        run: mvn clean package -DskipTests

      - name: Install SSH Key
        uses: shimataro/ssh-key-action@v2
        with:
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          known_hosts: 'placeholder'

      - name: Adding Known Hosts
        run: ssh-keyscan -H ${{ secrets.SSH_HOST }} >> ~/.ssh/known_hosts

      - name: Deploy and Start Application
        run: |
          scp target/*.jar ubuntu@${{ secrets.SSH_HOST }}:~/app.jar
          ssh ubuntu@${{ secrets.SSH_HOST }} "fuser -k 8080/tcp || true" 
          ssh ubuntu@${{ secrets.SSH_HOST }} "nohup java -jar ~/app.jar > log.txt 2>&1 &"